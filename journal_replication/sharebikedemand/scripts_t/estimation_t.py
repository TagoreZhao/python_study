# We will need to load all libraries here

# We start with section
def gen_sync_in_grid() #We first need figure out what parameters we are gonna need and output.
    print("This function is used to generate our syntheyic data.")